# The list of candies to print to the screen
piesList = ["Pecan", "Apple Crisp", "Bean", "Banoffee", "Black Bun",
 "Blueberry", "Buko", "Burek", "Tamale", "Steak"]
pieCart = {}
shopping = 'y'

# Enter the piesList
for pie in piesList:
    print("[" + str(piesList.index(pie)) + "]" + pie)

# Then prompt the user to select which pie they'd like to order via number.
pie = int(input("Enter Pie number "))

# Immediately after, follow the order with `Great! We'll have that <PIE NAME> right out for you.` and then ask if they would like to make another order. If so, repeat the process.
print(f"Great! We'll have that ${pie} right out for you.")

# Once the user is done purchasing pies, print the total number of pies ordered.
if pieCart[piesList[pie]] = :
    pass
else:
    pass  = 1
    candyValue = int(input("Which candy would you like to bring "))

for candy in candyCart:
    print("[" + str(candyCart.index(candy)) + "]" + candy)

